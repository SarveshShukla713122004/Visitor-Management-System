# Environment Configuration

# Node Environment
NODE_ENV=production

# Server Port
PORT=5000

# MongoDB Connection String (Replace with production MongoDB URI, e.g., MongoDB Atlas)
# MONGO_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/mecon_vms?retryWrites=true&w=majority
# If MONGO_URI is omitted, the system defaults to in-memory MongoDB for easy demonstration.

# JWT Authentication Secret (Use a strong random secret in production)
JWT_SECRET=mecon_super_secret_jwt_key_2026_prod_v2
JWT_EXPIRE=7d

# Frontend Origin URL for CORS
CLIENT_URL=http://localhost:5174
